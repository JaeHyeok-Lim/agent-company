# Claude Code local/session state
.claude/settings.local.json

# Live dashboard state (written by hooks at runtime)
.claude/state/

# Per-run staffing record — the chief-of-staff rewrites this on every /go run, so it's
# transient working state (twin of .claude/state/allocation.json), not a versioned doc.
docs/staffing.md

# Node (for the future TS SDK escalation path)
node_modules/
dist/
*.log
.env
.env.*
!.env.example

# OS
Thumbs.db
.DS_Store
